server:
  port: ${port:8501}

spring:
  application:
    name: eureka

eureka:
  client:
    serviceUrl:
      defaultZone: ${eureka.serviceUrl:http://127.0.0.1:8501/eureka/}
  instance:
    hostname: ${hostname:eureka}
